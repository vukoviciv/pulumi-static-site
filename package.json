{
  "name": "pulumi-s3",
  "scripts": {
    "build:client": "npm --prefix app run build",
    "pulumi:deploy:client": "bash scripts/deploy-client.sh",
    "deploy": "npm run deploy --prefix infrastructure && npm run pulumi:deploy:client"
  }
}
